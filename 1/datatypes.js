"use strict"; // Treat all code based on newer versionof js.

// alert("HI"); // we are using node.js,not browser.

console.log(
  3 + // Code Readability should be high.
    3
);

let name = "Mukul";
let age = 24;
let isLoggedIn = true;
let temp = null;
let state;

console.log(typeof(temp)); // null is an object
console.log(typeof temp);

// Primitive DataTypes.
// number => 2e53;
// bigint
// boolean => true/false;
// string => "";
// null => standalone value; (Null is the representation of an empty value,null does'nt mean "" or 0).
// undefined =>
// symbol => uniqueness.

